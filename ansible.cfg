[defaults]
# Enable the following if testing ec2 dynamic inventoryu from commandline
# enable_plugins = aws_ec2
# inventory = ./plugins
host_key_checking = false 
#stdout_callback = json
collections_paths = collections

# Plays will gather facts by default, which contain information about
# the remote system.
#
# smart - gather by default, but don't regather if already gathered
# implicit - gather by default, turn off with gather_facts: False
# explicit - do not gather by default, must say gather_facts: True
gathering = explicit

timeout = 60

# Turn off OK and SKIPPED tasks to show only CHANGED tasks for snow
#display_ok_hosts=false
#display_skipped_hosts=false

# Turning off host key checking 
host_key_checking = false

[inventory]
enable_plugins = host_list, script, auto, yaml, ini, toml, amazon.aws.aws_ec2